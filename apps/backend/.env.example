# Servidor
PORT=3001

# JWT (obrigatório para login e refresh token; mín. 32 caracteres; gerar: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-key-min-32-chars

# Opcional: expiração dos tokens (padrão: 15m e 7d no código)
# JWT_EXPIRES_IN=15m
# JWT_REFRESH_EXPIRES_IN=7d
